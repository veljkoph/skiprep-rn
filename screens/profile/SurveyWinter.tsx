import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SurveyWinter = () => {
  return (
    <View>
      <Text>SurveyWinter</Text>
    </View>
  );
};

export default SurveyWinter;

const styles = StyleSheet.create({});
